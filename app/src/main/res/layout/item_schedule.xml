<?xml version="1.0" encoding="utf-8"?>
<!-- 使用FrameLayout作为根布局实现图层叠加 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?android:attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">
    <ImageButton
        android:id="@+id/btn_delete"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@android:drawable/ic_delete"
        android:contentDescription="删除日程"
        android:layout_gravity="center_vertical|end"
        android:layout_marginEnd="80dp"
        android:background="?attr/selectableItemBackground"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
    <!-- 滑动背景层（左滑时显示的绿色背景） -->
    <LinearLayout
        android:id="@+id/swipe_background"
        android:layout_width="80dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="#4CAF50"
        android:gravity="center"
        android:orientation="horizontal"
        android:visibility="invisible">

        <!-- 白色对勾图标 -->
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_done_white"/>
    </LinearLayout>

    <!-- 内容层（整合所有需求控件） -->
    <FrameLayout
        android:id="@+id/swipe_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            android:paddingBottom="8dp">

            <!-- 标题行（整合标题TextView和完成状态CheckBox） -->
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <!-- 标题TextView（id改为tv_title） -->
                <TextView
                    android:id="@+id/tv_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_toStartOf="@+id/cb_completed"
                    android:textColor="#212121"
                    android:textSize="18sp"
                    android:textStyle="bold"/>

                <!-- 完成状态CheckBox（右上角） -->
                <CheckBox
                    android:id="@+id/cb_completed"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_alignParentEnd="true"
                    android:layout_marginEnd="8dp"
                    android:button="@null"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:drawableStart="@drawable/selector_done_checkbox"
                    android:drawablePadding="0dp"/>
            </RelativeLayout>

            <!-- 时间范围TextView（替代原有的日期+时间组合） -->
            <TextView
                android:id="@+id/tv_time_range"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textColor="#666666"
                android:textSize="16sp"/>

            <!-- 保留原有的日期和时间显示（可选，根据需求决定是否保留） -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:visibility="gone"> <!-- 默认隐藏 -->

                <!-- 日期图标 -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingEnd="4dp"
                    android:text="📅"
                    android:textSize="14sp"/>

                <TextView
                    android:id="@+id/tv_schedule_date"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="#666666"
                    android:textSize="16sp"/>

                <!-- 分隔点 -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingStart="8dp"
                    android:paddingEnd="8dp"
                    android:text="•"
                    android:textColor="#999999"
                    android:textSize="16sp"/>

                <!-- 时间图标 -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingEnd="4dp"
                    android:text="⏰"
                    android:textSize="14sp"/>

                <TextView
                    android:id="@+id/tv_schedule_time"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="#666666"
                    android:textSize="16sp"/>
            </LinearLayout>
        </LinearLayout>
    </FrameLayout>
</FrameLayout>